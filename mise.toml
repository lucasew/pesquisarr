[env]
_.path = ["./node_modules/.bin"]

[tools]
node = "26.3.0"

[hooks.postinstall]
script = "npm install"

[settings]
experimental = true

[tasks.lint]
run = "npm run lint"

[tasks.ci]
depends = ["lint"]
